Create->Add->Commit->Push
Any Modification->Add->Commit->Push

######### git_commands
git --version
git config --global user.name "****"
git config --global user.email "***@***"
git config --list
git clone <--link-->
git pull
git status
git add <--file name--> or git add .
git commit -m "some msg"
git push origin main
git branch                                  -> to check branch 
git branch -a                               ->to check file in the branch
git diff <--branch_name-->                  ->gives the differences of the two branches
git branch -M main                          ->to rename branch
git checkout <--branchname-->               ->to navigate
git checkout -b <--new branch name-->       -> to create new branch name
git branch -d <--branch name-->             ->to delete branch name
git merge main
git init                                    ->making normal folder in to git repository
git add .
git remote add origin <--path-->            ->create local folder make it as git repo and create repo in git hub and connect
git pull origin main                        ->
git branch <--new_branch-->                 ->create new_branch
git push origin <--new_branch>
pull request and pull request conflict??


